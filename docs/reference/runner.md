# Runner

The `SetupRunner` class is the central orchestrator for all deployments. Import it from the `core` subpackage:

```python
from vm_tool.core.runner import SetupRunner, SetupRunnerConfig, SSHConfig
```

::: vm_tool.core.runner
